<%= render 'admins/navigation'%>
<%= render 'admins/sidebar'%>
<br/><br/><br/>
<%= form_for @profile do |f| %>
<div class="col-sm-2">
	</div>
<div class="col-sm-9">
	<div class="col-lg-12">
				<div class="panel panel-default">
					<div class="panel-heading">Edit Profil</div>
					<br/>
	<form class="form-horizontal" role="form">
		<div class="form-group">
			<%= f.label :content, "Isi Profile : ", :class => "control-label col-sm-2" %>
			<div class="col-sm-10">
				<%= f.text_area :content, :rows => '15',:class => 'form-control', :placeholder=>'Masukan Isi Profile'%>				
			</div>			
				<%= f.submit "Simpan", :class => 'btn btn-default'%>		
				</form>
			</div>
		</div>
	</div>
	<% end %>
	
